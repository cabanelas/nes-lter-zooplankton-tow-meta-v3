# data/processed/

Generated pipeline outputs (`R/01`–`R/05`) → NES-LTER bongo TDR EDI package.
Regenerate via the numbered scripts in `R/`.

Outputs aren't tracked in git — they're reproducible from the scripts, and the final versions are in the published EDI package.